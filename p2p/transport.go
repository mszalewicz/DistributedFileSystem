package p2p

// Peer is interface that represents remote node.
type Peer interface {}

// Transport is interface that handles communication between nodes.
// Which encompasses { TCP, UDP, websocket}
type Transport interface {}

